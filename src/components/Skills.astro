---
const skillCategories = [
  {
    title: "ML Development",
    skills: [
      "PyTorch, TensorFlow, Keras",
      "Python: NumPy, pandas, scikit-learn",
      "BERT, XLNet, GPT, Hugging Face",
      "Fine-tuning and domain adaptation",
      "Deep semi-supervised learning",
      "NLU, generation, image to text",
    ],
  },
  {
    title: "Data",
    skills: [
      "Human evaluation at scale",
      "Crowdsourcing pipeline design",
      "Custom metric development",
      "Data augmentation and corpus construction",
      "Resource-efficient ML pipeline",
    ],
  },
  {
    title: "Production & Communication",
    skills: [
      "Git, experiment tracking",
      "Scientific writing, publication, presentation, documentation",
    ],
  },
];
---

<section id="skills" class="py-10 border-t border-slate-800">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-2xl font-bold text-slate-100 mb-6">Skills</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {skillCategories.map((category) => (
        <div>
          <h3 class="text-sm font-semibold text-primary-400 uppercase tracking-wide mb-3">
            {category.title}
          </h3>
          <ul class="space-y-1.5 text-sm text-slate-400">
            {category.skills.map((skill) => (
              <li class="flex items-start">
                <span class="text-primary-500 mr-2">•</span>
                {skill}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>
